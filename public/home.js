//redirection from homepage to a room
//This could be a new room generated by the user through "create meeting room!" option
//or could be one created by putting a customised meeting id through "Enter roon ID" option
//A user can also join an existing room by entering a room ID of an existing room shared with them
const room_id_inp = document.querySelector("#room-id");
if(room_id_inp) room_id_inp.addEventListener('keypress', (e)=>{
    if(e.key=='Enter') window.location.replace('/'+room_id_inp.value+'?email='+EmailId);
});
let redirect = ()=>{
    console.log("clicked")
    window.location.replace('/meet?email='+EmailId);
}

//Illustration - Using VanillaTilt JS library to add interactivity in the illustration on the homepage
VanillaTilt.init(document.querySelector(".illustration"), {
	max: 10,
	speed: 100,
	reverse: true,
	axis: "x",
	"full-page-listening": true
});

